# Audio capture & VAD
pyaudio>=0.2.14
torch>=2.0.0          # Silero VAD runs on torch (CPU fine)
torchaudio>=2.0.0

# Speech-to-text
faster-whisper>=1.0.0

# Command matching
rapidfuzz>=3.0.0
sentence-transformers>=2.7.0   # all-MiniLM-L6-v2 (~80MB)

# LLM fallback (choose one backend)
#llama-cpp-python>=0.2.0        # llama.cpp Python bindings

# Utilities
numpy>=1.24.0
paho-mqtt~=2.1.0
